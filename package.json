{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "disposables.email",
  "version": "1.0.0",
  "description": "An open-source database of disposable email domains",
  "scripts": {
    "dev": "bun run --hot ./src/server.ts",
    "build": "bun run ./src/build.ts"
  },
  "dependencies": {
    "hono": "^4.8.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.16"
  }
}
